import type { Project } from '@/types/project';

export interface ProjectCardProps {
  project: Project;
  onSelect?: () => void;
  onDelete?: () => void;
}

export function ProjectCard(props: ProjectCardProps) {
  // TODO: Implement project card component
  return null;
}
