export interface EditorProps {
  projectId: string;
}

export function Editor(props: EditorProps) {
  // TODO: Implement main editor component
  // Should compose Timeline, Preview, MediaLibrary, etc.
  return null;
}
