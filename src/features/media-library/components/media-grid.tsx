import type { MediaMetadata } from '@/types/storage';

export interface MediaGridProps {
  items: MediaMetadata[];
}

export function MediaGrid(props: MediaGridProps) {
  // TODO: Implement media grid component
  return null;
}
