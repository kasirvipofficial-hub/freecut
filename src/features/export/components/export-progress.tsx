import type { RenderProgress } from '../types';

export interface ExportProgressProps {
  progress: RenderProgress;
  onCancel?: () => void;
}

export function ExportProgress(props: ExportProgressProps) {
  // TODO: Implement export progress component
  return null;
}
