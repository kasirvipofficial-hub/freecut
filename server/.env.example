# Render Server Environment Configuration
# Copy this to .env in the server directory for local development
# For Railway/production, set these as environment variables in the dashboard

# Server port
PORT=3001

# Environment mode (development, production)
NODE_ENV=production

# CORS allowed origins (comma-separated for multiple)
# Example: https://your-app.vercel.app,https://custom-domain.com
CORS_ORIGIN=https://your-app.vercel.app

# Host binding
# Use 0.0.0.0 for containers (Railway, Docker)
# Use 127.0.0.1 for local-only access
HOST=0.0.0.0

# Optional: Chromium path (auto-detected in Docker)
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
# REMOTION_CHROME_EXECUTABLE=/usr/bin/chromium
